% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/word_html_split.R
\name{word_html_split}
\alias{word_html_split}
\title{Split an HTML document into multiple files based on heading levels}
\usage{
word_html_split(
  input_html,
  output_dir = "components",
  heading_tag = "h1",
  wrap_html = FALSE
)
}
\arguments{
\item{input_html}{A file path or a parsed \code{xml2::xml_document}.}

\item{output_dir}{Directory where split HTML files will be written.}

\item{heading_tag}{A heading tag ("h1", "h2", etc.) used to define sections.}

\item{wrap_html}{Logical; if TRUE, each section is written as a minimal
HTML document. If FALSE, the raw fragment is written.}
}
\value{
Invisibly returns \code{TRUE}. Called for its side effect of writing files.
}
\description{
Takes an HTML document (Word-generated or otherwise) and splits it
into multiple component files. Each component begins at a specified
heading tag (e.g. "h1") and continues until the next such heading.
}
\examples{
\dontrun{
word_html_split("document.html", output_dir = "components")
}

}
